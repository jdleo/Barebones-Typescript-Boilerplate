describe('First test component', () => {
  it('should be true when true', () => {
    expect(true).toBe(true);
  });

  it('should be false when false', () => {
    expect(false).toBe(false);
  });
});

describe('Second test component', () => {
  it('should be 4 when 2+2', () => {
    expect(2 + 2).toBe(4);
  });

  it('should be 30 when 5*6', () => {
    expect(5 * 6).toBe(30);
  });
});
